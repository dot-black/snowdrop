<div class="panel-body">
  <div class="row">
    <% @products.each do |product| %>
      <%= render partial: "product", locals: { product: product } %>
    <% end %>
  </div>
</div>
<%= paginate @products %>
