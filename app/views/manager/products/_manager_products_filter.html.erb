<ul class="nav nav-tabs">
  <% products_categories_links.each do |key,link| %>
    <%= content_tag :li, class: "#{'active' if key.to_s == @current_category}" do %>
      <%= link_to key.capitalize, link, remote: true %>
    <% end %>
  <% end %>
</ul>
