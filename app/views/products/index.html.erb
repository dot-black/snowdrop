<div class=category-item>
  <%= image_tag "categories/mens_outerwear.jpg", class: "category-image" %>
    <h2> Men's Outerwear </h2>
    <span><%= "(#{pluralize @products.count, 'item'})" %></span>
<div>
<div class="products">
  <% @products.each do |product| %>
    <%= render partial: "product", locals: { product: product } %>
  <% end %>
</div>
