<table class="body-wrap">
  <tr>
    <td></td>
    <td class="container" width="600">
      <div class="content">
        <table class="main" width="100%" cellpadding="0" cellspacing="0">
          <tr>
            <td class="content-wrap">
              <table cellpadding="0" cellspacing="0">
                <tr>
                  <td>
                    <%= link_to store_url do  %>
                      <%= image_tag (image_path 'mailer/header.png'), class:"img-responsive"  %>
                    <% end %>
                  </td>
                </tr>
                <tr>
                  <td class="content-block">
                    <h3>Your order is <%= @order.status %>.</h3>
                  </td>
                </tr>
                <tr>
                  <td class="content-block">
                    You've received this email because status of your order is changed.<br>
                    Details of your order are below:
                  </td>
                </tr>
                <tr>
                  <td class="content-block">
                    <table class="invoice-items" cellpadding="0" cellspacing="10px">
                      <% @order.line_items.each do |line_item| %>
                        <tr>
                          <td>
                            <%= link_to product_url(line_item.product) do %>
                              <%= image_tag( (line_item.product.images == [] ?  image_path("default/default_product_image.png") : image_path(line_item.product.images.first)),size: 64) %>
                            <% end %>
                          </td>
                          <td>
                            <%= line_item.quantity %>x <%= line_item.product.title %> <br>
                            Size: <%= line_item.size.values.join(" / ") %> <br>
                            <%= number_to_currency(line_item.quantity * line_item.product.price) %>
                          </td>
                        </tr>
                      <% end %>
                    </table>
                    <br>
                    <p> <strong>Total: <%= number_to_currency @order.amount %></strong></p>
                  </td>
                </td>
              </table>
            </td>
          </tr>
        </table>
      </div>
    </td>
  </tr>
</table>
<div class="footer">
  <table width="100%">
    <tr>
      <td class="aligncenter content-block">Visit <%= link_to "ARI DAR", store_url(locale: I18n.locale) %> site.</td>
    </tr>
  </table>
</div>
