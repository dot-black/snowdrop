<div class="container">
  <div class="order-headline">
    <h2><%= t '.headline' %></h2>
    <span>
      <%= link_to("#{t '.change_email'}",new_user_path) %>
      <%= link_to(" #{t '.back_to_order'}",new_order_path) if session[:user_information_id].present? %>
    </span>
  </div>
  <%= render "form"%>
</div>
